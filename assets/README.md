Bundled public assets live here.

On startup, Duckflix copies each top-level assets from this folder into `PUBLIC_FOLDER`.
For profile avatars, place files under:

```txt
assets/
```

Those files will be copied to:

```txt
${PUBLIC_FOLDER}/
```
